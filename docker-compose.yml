version: "3.8"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=docker-compose-secret-key-replace-in-production
      - FLASK_DEBUG=1
    volumes:
      - uploads:/app/uploads
      - db_data:/app/instance

volumes:
  uploads:
  db_data:
